# Security

This is a local, single-user research application. Do not expose the Python server directly to the public internet. GitHub Pages serves only a static replay and does not accept model credentials.

Never include API keys, private novels or full provider responses in public issues. Report ordinary bugs with synthetic text and a redacted reproduction. For a security issue, use GitHub's private vulnerability reporting if available on the repository.
